import 'package:flutter/material.dart';

class NewPoastScreen extends StatelessWidget {
  const NewPoastScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
